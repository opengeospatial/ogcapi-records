---
ServerError:
  description: |-
    A server error occurred.
  content:
    application/json:
      schema:
        $ref: '../schemas/common/exception.yaml'
    text/html:
      schema:
        type: string
