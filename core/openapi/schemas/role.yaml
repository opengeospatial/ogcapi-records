type: object
description:
  The function performed by the contact. Suggested codelists include the
  STAC Collection provider roles as well as ISO 19115.
required:
  - name
properties:
  name:
    type: string
  authority:
    type: string
    format: uri
