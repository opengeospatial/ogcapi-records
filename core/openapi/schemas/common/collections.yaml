---
allOf:
  - $ref: links.yaml
  - type: object
    required:
      - links
      - collections
    properties:
      collections:
        type: array
        items:
          $ref: collectionInfo.yaml
