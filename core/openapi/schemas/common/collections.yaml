---
type: object
required:
  - links
  - collections
properties:
  links:
    type: array
    items:
      oneOf:
        - $ref: link.yaml
        - $ref: linkTemplate.yaml
  collections:
    type: array
    items:
      $ref: collectionInfo.yaml
