---
allOf:
  - $ref: common/links.yaml
  - type: object
    required:
      - links
      - collections
    properties:
      collections:
        type: array
        items:
          $ref: catalog.yaml
