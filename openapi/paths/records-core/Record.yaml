get:
  tags:
    - Record
  summary:
    Fetch a single record.
  description:
    Fetch the record with id `recordId` in the catalog with id `catalogId`.
    Use content negotiation to request HTML or GeoJSON.
  operationId: getRecord
  parameters:
    - $ref: '../../parameters/records-core/catalogId.yaml'
    - $ref: '../../parameters/records-core/recordId.yaml'
    - $ref: '../../parameters/records-core/language.yaml'
    - $ref: '../../parameters/unspecified/f.yaml'
  responses:
    '200':
      $ref: '../../responses/records-core/Record.yaml'
    '404':
      $ref: '../../responses/features-core/NotFound.yaml'
    '500':
      $ref: '../../responses/features-core/ServerError.yaml'
