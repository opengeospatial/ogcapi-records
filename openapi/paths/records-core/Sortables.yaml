get:
  tags:
    - Sortables
  summary: |-
   get the list of sortable properties
  description: |-
    Fetch the list of properties which can be used to sort the getRecords
    response.
  operationId: getSortables
  parameters:
    - $ref: '../../parameters/records-core/catalogId.yaml'
    - $ref: '../../parameters/unspecified/f.yaml'
  responses:
    '200':
      $ref: '../../responses/records-core/Sortables.yaml'
    '404':
      $ref: '../../responses/features-core/NotFound.yaml'
    '500':
      $ref: '../../responses/features-core/ServerError.yaml'
