---
description:
  Information about the catalog with id `catalogId`.
content:
  application/json:
    schema:
      $ref: '../../schemas/records-core/catalog.yaml'
  text/html:
    schema:
      type: string
