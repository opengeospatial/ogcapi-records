[[req_harvest_get-op]]
[width="90%",cols="2,6a"]
|===
^|*Requirement {counter:req-id}* |*/req/harvest/get-op*
^|A |For every catalog (path: `/collections/{collectionId}`) where the `itemType` is `record`, the server SHALL support the HTTP GET operation at the path `/collections/{catalogId}/harvest`.
|===
